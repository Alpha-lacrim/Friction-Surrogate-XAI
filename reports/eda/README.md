# EDA Outputs

Generated EDA artifacts are written here by `python -m friction_surrogate_xai.eda`.

The generated plot, table, markdown, and CSV outputs are intentionally ignored by Git and logged to MLflow as artifacts.

